{
  "name": "korfbal-live-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "concurrently -n astro,icons \"astro dev\" \"npm run generate:icons:watch\"",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate:icons": "node scripts/generate-icons.mjs",
    "generate:icons:watch": "node scripts/generate-icons.mjs --watch"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.1",
    "gotham-fonts": "^1.0.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
