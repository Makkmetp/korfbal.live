<header class="navbar">
  <div class="navbar__element">
    <a class="navbar__brand" href="/" aria-label="Ga naar homepage">
      <img src="images/kl-logo-horizontal.svg" alt="Korfbal.live">
    </a>

    <nav class="navbar__nav" aria-label="Hoofdnavigatie">
      <ul class="navbar__list" role="list">
        <li><a class="navbar__link" href="#overzicht">Overzicht</a></li>
        <li><a class="navbar__link" href="#liveverslag">Liveverslag</a></li>
        <li><a class="navbar__link" href="#styles">Design system</a></li>
      </ul>
    </nav>

    <a class="navbar__cta" href="#overzicht">Start gratis</a>
  </div>
</header>

<style>

  .navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 20;
  }

  .navbar__element {
    --_navbar-wrap: nowrap;
    --_navbar-justify: space-between;
    --_navbar-row-gap: 0;
    --_navbar-padding-block: 0;
    --_navbar-nav-order: 0;
    --_navbar-nav-width: auto;
    --_navbar-nav-margin-inline: auto;
    --_navbar-list-justify: flex-start;
    --_navbar-list-wrap: nowrap;

    width: calc(100% - (var(--nav-padding-x) * 2));
    max-width: 64rem;
    min-height: var(--nav-height);
    margin-inline: auto;
    display: flex;
    align-items: center;
    justify-content: var(--_navbar-justify);
    gap: var(--space-3);
    flex-wrap: var(--_navbar-wrap);
    row-gap: var(--_navbar-row-gap);
    container-type: inline-size;
    background-color: white;
    border-radius: var(--nav-radius);
    box-shadow: var(--theme-shadow-subtle);
    margin-top: 3rem;
    padding-inline: var(--nav-padding-x);
    padding-block: var(--_navbar-padding-block);
  }

  .navbar__brand {
    font-family: var(--font-family-base);
    font-weight: var(--font-weight-bold);
    font-size: var(--font-size-body-lg);
    color: var(--theme-color-text);
    line-height: 1;
    white-space: nowrap;
    max-width: 10rem;
  }

  .navbar__nav {
    order: var(--_navbar-nav-order);
    width: var(--_navbar-nav-width);
    margin-inline: var(--_navbar-nav-margin-inline);
  }

  .navbar__list {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    justify-content: var(--_navbar-list-justify);
    flex-wrap: var(--_navbar-list-wrap);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .navbar__link {
    color: var(--theme-link-text);
    font-size: var(--font-size-body-sm);
    font-weight: var(--font-weight-medium);
    border-bottom: 2px solid transparent;
    padding-bottom: 0.2em;
    transition:
      color 0.2s ease,
      border-color 0.2s ease;
  }

  .navbar__link:hover,
  .navbar__link:focus-visible {
    color: var(--theme-link-text-hover);
    border-color: var(--theme-link-border-hover);
  }

  .navbar__cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 2.4rem;
    padding-inline: var(--space-3);
    border: 1px solid var(--theme-button-primary-border);
    border-radius: var(--button-radius);
    background: var(--theme-button-primary-bg);
    color: var(--theme-button-primary-text);
    font-size: var(--font-size-body-sm);
    font-weight: var(--font-weight-medium);
    white-space: nowrap;
    transition:
      background-color 0.2s ease,
      color 0.2s ease,
      border-color 0.2s ease;
  }

  .navbar__cta:hover,
  .navbar__cta:focus-visible {
    background: var(--theme-button-primary-bg-hover);
    color: var(--theme-button-primary-text-hover);
    border-color: var(--theme-button-primary-bg-hover);
  }

  @container (width < 50em) {
    .navbar__element {
      --_navbar-wrap: wrap;
      --_navbar-justify: center;
      --_navbar-row-gap: var(--space-2);
      --_navbar-padding-block: var(--space-2);
      --_navbar-nav-order: 3;
      --_navbar-nav-width: 100%;
      --_navbar-nav-margin-inline: 0;
      --_navbar-list-justify: center;
      --_navbar-list-wrap: wrap;
    }
  }
</style>
