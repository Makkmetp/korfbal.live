---
interface Props {
  size?: number | string;
  strokeWidth?: number | string;
  class?: string;
  title?: string;
  decorative?: boolean;
}

const {
  size = 24,
  strokeWidth = 1.5,
  class: className = "",
  title = "Menu",
  decorative = true,
} = Astro.props;
---

<svg class={className} xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="currentColor"
  aria-hidden={decorative ? "true" : "false"}
  role={decorative ? undefined : "img"}>
  {!decorative && <title>{title}</title>}<path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M21 6a1 1 0 0 1 -1 1h-16a1 1 0 1 1 0 -2h16a1 1 0 0 1 1 1" /><path d="M21 12a1 1 0 0 1 -1 1h-16a1 1 0 0 1 0 -2h16a1 1 0 0 1 1 1" /><path d="M21 18a1 1 0 0 1 -1 1h-16a1 1 0 0 1 0 -2h16a1 1 0 0 1 1 1" /></svg>